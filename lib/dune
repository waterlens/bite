(menhir
  (modules tokens)
  (flags --only-tokens)
)

(menhir
  (modules tokens parser)
  (merge_into parser)
  (flags --external-tokens Tokens --dump --explain)
)

(ocamllex lexer)

(library
 (name bite)
 (modules parser lexer tokens))
